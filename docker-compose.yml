services:          
    relationalgraph.api:
        image: gabrielsouz/MindNose
        restart: unless-stopped
        container_name: MindNose
        ports:
            - "${APIHTTPPORT}:8080"
        environment:
            - DBHOST=${DBHOST}
            - DBUSER=${DBUSER}
            - DBPASSWORD=${DBPASSWORD}
            - ASPNETCORE_URLS=http://0.0.0.0:8080

volumes:
    data:
    logs: